# Base URL of the Korugan Go API.
NEXT_PUBLIC_API_BASE=http://localhost:8080

# Bearer token, only if the backend has KORUGAN_API_TOKEN set.
# Note: exposed to the browser (NEXT_PUBLIC_*). Fine for a self-hosted
# single-operator console; real per-user auth arrives with session login.
NEXT_PUBLIC_API_TOKEN=
